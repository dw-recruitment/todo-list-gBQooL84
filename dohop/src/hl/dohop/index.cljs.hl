(page "index.html"
  (:require
    [dohop.rpc :as rpc]))

(rpc/init)

(html
  (head
    (link :href "app.css" :rel "stylesheet"))
  (body
    (h1 "Do List Navigator")
    (img :src "uconst.gif"
         :alt "Under Construction")))
