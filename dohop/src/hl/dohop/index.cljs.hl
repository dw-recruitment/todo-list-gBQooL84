(page "index.html"
  (:require
    [dohop.rpc :as rpc]))

(rpc/init)

(html
  (head
    (link :href "app.css" :rel "stylesheet"))
  (body
    (h1 (text "Hello, Hoplon and Castra! ~{rpc/random-number}"))))
